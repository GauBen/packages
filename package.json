{
  "name": "@gauben/packages",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all --topological-dev run build"
  },
  "devDependencies": {
    "@chachalog/types": "^0.1.0"
  },
  "packageManager": "yarn@4.7.0"
}
